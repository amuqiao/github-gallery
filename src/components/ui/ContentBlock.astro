---
interface Props {
  headingId: string;
  title: string;
}

const { headingId, title } = Astro.props;
---

<section class="content-block" aria-labelledby={headingId}>
  <h2 id={headingId} class="content-block__title">{title}</h2>
  <slot />
</section>
