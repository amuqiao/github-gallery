---
interface Props {
  headingId: string;
  title: string;
}

const { headingId, title } = Astro.props;
---

<section class="max-w-3xl border-t pt-5" aria-labelledby={headingId}>
  <h2 id={headingId} class="mb-2.5 text-xl font-semibold leading-tight">{title}</h2>
  <slot />
</section>
