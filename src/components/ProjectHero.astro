---
import PageHeader from "@/components/PageHeader.astro";
import Badge from "@/components/ui/Badge.astro";
import Button from "@/components/ui/Button.astro";
import type { Project, TaxonomyItem } from "@/lib/catalog/projects";

interface Props {
  project: Project;
  category: TaxonomyItem;
  tags: TaxonomyItem[];
}

const { project, category, tags } = Astro.props;
---

<PageHeader eyebrow={category.name} title={project.name} description={project.summary} variant="project" asideLabel="Project actions">
  <Fragment slot="meta">
    <span>Status: {project.status}</span>
    {project.meta?.license && <span>License: {project.meta.license}</span>}
    {project.meta?.languages && <span>Languages: {project.meta.languages.join(", ")}</span>}
  </Fragment>
  <Fragment slot="footer">
    <div class="flex flex-wrap gap-1.5">
      {tags.map((tag) => <Badge href={`/tags/${tag.id}/`} variant="secondary">{tag.name}</Badge>)}
    </div>
  </Fragment>
  <Fragment slot="actions">
    <Button href={project.repo}>Open repository</Button>
    <Button href={`/categories/${category.id}/`} variant="outline">View category</Button>
  </Fragment>
</PageHeader>
