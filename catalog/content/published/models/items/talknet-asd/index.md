# TalkNet-ASD

TalkNet-ASD 是用于主动说话人检测的音视频模型，目标是在多脸同屏的视频中判断哪一张脸正在说话。

它将音频片段和人脸视觉轨迹分别编码，再通过跨模态交互和长时序建模聚合说话证据，输出候选人脸在各时间片上的 active speaker 判断。

学习 TalkNet-ASD 时，重点应放在音频、视觉、人脸轨迹、时序窗口和跨模态注意力如何共同形成判断，而不是把它当成普通语音识别或人脸识别模型。
