# InsightFace

InsightFace 是一个开源人脸分析工具箱，面向人脸检测、关键点定位、对齐、特征提取、身份比对和相关 2D/3D 人脸任务。

理解 InsightFace 时，不应只把它看作一个模型仓库。更准确的主线是：一张输入图像先经过人脸检测和关键点定位，随后完成对齐与裁剪，再由识别模型生成 embedding，最后通过相似度、阈值、索引库或业务规则完成身份判定。

这类系统的工程重点在于端到端能力契约：输入图像质量、模型包选择、阈值校准、离线人脸库构建、在线推理链路和误识别风险都会影响最终结果。
