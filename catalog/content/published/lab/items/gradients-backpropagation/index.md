# 梯度与反向传播

梯度描述函数在参数空间中上升最快的方向，反向传播则利用链式法则在计算图上高效计算每个参数对损失的影响。它们共同构成神经网络训练的核心机制。

## 适合关注什么

- 导数、偏导数、梯度、Jacobian 和链式法则的关系。
- 前向计算图、反向梯度传播和参数更新的顺序。
- 梯度消失、梯度爆炸、截断、归一化和残差连接等稳定性手段。

## 使用边界

反向传播是求梯度的算法，不等同于优化器本身。SGD、Adam、学习率调度、权重衰减和梯度裁剪会继续决定参数如何被更新。
