# ByteTrack

ByteTrack 是一个多目标跟踪方法，核心思想是不要过早丢弃低置信度检测框，而是让已有轨迹和运动预测参与二次关联。

它关注的是视频中目标 ID 如何跨帧保持连续：检测器每帧给出候选框和分数，跟踪器把高分框先用于可靠匹配，再尝试用低分框恢复被遮挡或得分下降的真实目标，从而减少断轨和 ID switch。

学习 ByteTrack 时，主线应放在检测框、轨迹状态、匹配顺序、阈值和遮挡恢复，而不是把它当成一个独立检测模型。
