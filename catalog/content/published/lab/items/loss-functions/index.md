# 损失函数

损失函数把模型预测和训练目标之间的差距变成可优化的数值信号。它决定梯度如何更新参数，也影响模型更重视哪些错误和训练过程是否稳定。

## 适合关注什么

- 回归任务中的 MSE、MAE、Huber 等损失。
- 分类任务中的交叉熵、二分类交叉熵和 softmax/logits 关系。
- 损失函数、目标函数、正则化项和评估指标之间的区别。

## 使用边界

损失函数服务于训练优化，评估指标服务于业务或任务效果判断。两者可以相关，但不应混为一谈；实际项目常需要同时设计可优化目标和可解释评估指标。
