# 激活函数

激活函数为神经网络引入非线性，使多层网络不再退化为线性变换。它也直接影响梯度流、训练稳定性、表达能力，以及输出层如何匹配具体任务。

## 适合关注什么

- Sigmoid、Tanh、ReLU、Leaky ReLU、GELU 和 Softmax 的用途差异。
- 饱和区、梯度消失、死亡 ReLU 和数值稳定性问题。
- 隐藏层激活函数与输出层激活函数的选择逻辑。

## 使用边界

激活函数不是孤立超参数。选择时需要结合网络深度、初始化、归一化、损失函数、输出语义和任务类型一起判断。
