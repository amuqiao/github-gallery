# FFmpeg

FFmpeg 是多媒体处理领域的基础设施级工具集。它把解封装、解码、滤镜处理、编码、封装和推流等能力收拢到统一命令行与 libav* 库中，适合用来理解音视频处理链路的基本构件。

## 适合关注什么

- 多媒体文件从容器到音视频流再到编码格式的处理路径。
- `ffmpeg`、`ffprobe`、滤镜图和 libav* 组件之间的职责分工。
- 转码、裁剪、抽帧、混流、推流、字幕和批处理自动化场景。

## 使用边界

FFmpeg 更像底层能力与命令组合平台，不是开箱即用的产品界面。真实业务中通常需要在参数管理、任务编排、失败重试、资源隔离和结果校验上再封装一层。
